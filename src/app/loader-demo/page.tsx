"use client";

import React from 'react';
import Loader from '@/app/components/loader';

const LoaderDemo = () => {
  return (
    <Loader message="Place customized loading message here" />
  );
};

export default LoaderDemo;